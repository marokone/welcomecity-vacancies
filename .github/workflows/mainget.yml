name: Fetch FriendWork Jobs Full

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  fetch-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug FriendWork API response
        run: |
          resp=$(curl -s --request GET \
            --header 'Accept: application/json' \
            --header 'Content-Type: application/json' \
            --header "Authorization: Bearer ${{ secrets.FRIENDWORK_API_TOKEN }}" \
            --data "{\"statuses\": [\"open\", \"onHold\"], \"paging\": { \"page\": 0, \"count\": 1 }}" \
            'https://api.friend.work/jobs')
          echo "$resp"

      - name: Fetch all FriendWork jobs and details
        run: |
          page=0
          count=50
          jobids=()
          while :; do
            resp=$(curl -s --request GET \
              --header 'Accept: application/json' \
              --header 'Content-Type: application/json' \
              --header "Authorization: Bearer ${{ secrets.FRIENDWORK_API_TOKEN }}" \
              --data "{\"statuses\": [\"open\", \"onHold\"], \"paging\": { \"page\": $page, \"count\": $count }}" \
              'https://api.friend.work/jobs')
            items=$(echo "$resp" | jq '.Items')
            n=$(echo "$items" | jq 'length')
            if [ "$n" -eq 0 ]; then
              break
            fi
            ids=$(echo "$items" | jq -r '.[].jobId')
            for id in $ids; do
              jobids+=($id)
            done
            page=$((page+1))
          done
          echo "✅ Найдено вакансий: ${#jobids[@]}"
          
          details=()
          for jobid in "${jobids[@]}"; do
            echo "🔄 Загружаю детали вакансии $jobid..."
            detail=$(curl -s -X GET \
              --header 'Accept: application/json' \
              --header "Authorization: Bearer ${{ secrets.FRIENDWORK_API_TOKEN }}" \
              "https://api.friend.work/jobs/$jobid")
            if echo "$detail" | jq empty 2>/dev/null; then
              details+=("$detail")
            fi
          done
          printf '%s\n' "${details[@]}" | jq -s '.' > Beliy_Mys_VAC/jobs_full.json
          
          # Проверяем структуру полей в API
          echo "📊 Структура первой вакансии:"
          jq '.[0] | keys' Beliy_Mys_VAC/jobs_full.json
          
          echo "📊 Наличие новых полей (requirements, responsibilities, conditions):"
          jq '.[0] | {has_requirements: has("requirements"), has_responsibilities: has("responsibilities"), has_conditions: has("conditions")}' Beliy_Mys_VAC/jobs_full.json

      - name: Upload jobs_full.json
        uses: actions/upload-artifact@v4
        with:
          name: friendwork-jobs-full
          path: Beliy_Mys_VAC/jobs_full.json

      - name: Debug jobs_full.json
        run: |
          echo "📋 Первые 3 вакансии:"
          cat Beliy_Mys_VAC/jobs_full.json | jq '.[:3] | .[] | {jobId: .jobId, title: .name, has_requirements: has("requirements")}'

      - name: Установить Python и зависимости
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests  # Убрали beautifulsoup4 и lxml - они больше не нужны!

      # ❌ Шаг Парсинг описаний вакансий - УДАЛЕН! Больше не нужен

      - name: Экспорт для Supabase
        run: |
          python export_to_supabase.py
          echo "✅ После export_to_supabase.py:"
          if [ -f Beliy_Mys_VAC/jobs_supabase.csv ]; then
            echo "📊 jobs_supabase.csv создан"
            echo "📊 Первые 5 строк:"
            head -5 Beliy_Mys_VAC/jobs_supabase.csv
            echo "📊 Проверка наличия данных в requirements/responsibilities/conditions:"
            tail -n +2 Beliy_Mys_VAC/jobs_supabase.csv | cut -d',' -f7,8,9 | head -5
          else
            echo "❌ jobs_supabase.csv НЕ создан!"
          fi
        working-directory: ${{ github.workspace }}/Beliy_Mys_VAC

      - name: Преобразование в vacancies_rows.csv
        run: |
          python convert_to_vacancies_rows.py
          echo "✅ После convert_to_vacancies_rows.py:"
          if [ -f Beliy_Mys_VAC/vacancies_rows.csv ]; then
            echo "📊 vacancies_rows.csv создан"
            echo "📊 Первые 5 строк:"
            head -5 Beliy_Mys_VAC/vacancies_rows.csv
            echo "📊 Проверка наличия данных в requirements/responsibilities/conditions:"
            tail -n +2 Beliy_Mys_VAC/vacancies_rows.csv | cut -d',' -f6,7,8 | head -5
          else
            echo "❌ vacancies_rows.csv НЕ создан!"
          fi
        working-directory: ${{ github.workspace }}/Beliy_Mys_VAC

      - name: Upload jobs_supabase.csv
        uses: actions/upload-artifact@v4
        with:
          name: friendwork-jobs-supabase
          path: Beliy_Mys_VAC/jobs_supabase.csv

      - name: Загрузка данных в Supabase
        run: |
          python upload_to_supabase.py
          echo "✅ Загрузка в Supabase завершена"
        working-directory: ${{ github.workspace }}/Beliy_Mys_VAC

      - name: Upload vacancies_rows.csv
        uses: actions/upload-artifact@v4
        with:
          name: friendwork-vacancies-rows
          path: Beliy_Mys_VAC/vacancies_rows.csv

      # ❌ Upload jobs_structured.json - ТОЖЕ УДАЛЕН! Этот файл больше не создается
