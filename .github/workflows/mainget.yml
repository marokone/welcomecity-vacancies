name: Fetch FriendWork Jobs Full

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  fetch-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch all FriendWork jobs and details
        run: |
          page=0
          count=50
          jobids=()
          while :; do
            resp=$(curl -s --request GET \
              --header 'Accept: application/json' \
              --header 'Content-Type: application/json' \
              --header "Authorization: Bearer ${{ secrets.FRIENDWORK_API_TOKEN }}" \
              --data "{\"statuses\": [\"open\", \"onHold\"], \"paging\": { \"page\": $page, \"count\": $count }}" \
              'https://api.friend.work/jobs')
            items=$(echo "$resp" | jq '.Items')
            n=$(echo "$items" | jq 'length')
            if [ "$n" -eq 0 ]; then
              break
            fi
            ids=$(echo "$items" | jq -r '.[].jobId')
            for id in $ids; do
              jobids+=($id)
            done
            page=$((page+1))
          done

          echo "✅ Найдено вакансий: ${#jobids[@]}"

          # Сохраняем все детали в один файл
          echo "🔄 Загружаем детали вакансий..."
          echo '[' > Beliy_Mys_VAC/jobs_full.json
          first=1
          for jobid in "${jobids[@]}"; do
            echo "  Загружаю вакансию $jobid..."
            detail=$(curl -s -X GET \
              --header 'Accept: application/json' \
              --header "Authorization: Bearer ${{ secrets.FRIENDWORK_API_TOKEN }}" \
              "https://api.friend.work/jobs/$jobid")
            
            if echo "$detail" | jq empty 2>/dev/null; then
              if [ $first -eq 1 ]; then
                first=0
              else
                echo ',' >> Beliy_Mys_VAC/jobs_full.json
              fi
              echo "$detail" >> Beliy_Mys_VAC/jobs_full.json
              echo "    ✅ Успешно"
            else
              echo "    ❌ Ошибка: невалидный JSON"
            fi
          done
          echo ']' >> Beliy_Mys_VAC/jobs_full.json

      - name: Upload jobs_full.json
        uses: actions/upload-artifact@v4
        with:
          name: friendwork-jobs-full
          path: Beliy_Mys_VAC/jobs_full.json

      - name: Debug jobs_full.json
        run: |
          echo "📊 Статистика по кастомным полям:"
          jq '[.[].customFieldValues[] | select(.SystemName | startswith("Toruk_Job_")) | .SystemName] | unique' Beliy_Mys_VAC/jobs_full.json
          
          echo ""
          echo "📊 Количество вакансий с полями:"
          echo "  Requirements: $(jq '[.[].customFieldValues[] | select(.SystemName=="Toruk_Job_Requirements")] | length' Beliy_Mys_VAC/jobs_full.json)"
          echo "  Responsibilities: $(jq '[.[].customFieldValues[] | select(.SystemName=="Toruk_Job_Responsibilities")] | length' Beliy_Mys_VAC/jobs_full.json)"
          echo "  Conditions: $(jq '[.[].customFieldValues[] | select(.SystemName=="Toruk_Job_Conditions")] | length' Beliy_Mys_VAC/jobs_full.json)"

      - name: Установить Python и зависимости
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests

      - name: Экспорт для Supabase
        run: python export_to_supabase.py
        working-directory: Beliy_Mys_VAC

      - name: Преобразование в vacancies_rows.csv
        run: python convert_to_vacancies_rows.py
        working-directory: Beliy_Mys_VAC

      - name: Upload jobs_supabase.csv
        uses: actions/upload-artifact@v4
        with:
          name: friendwork-jobs-supabase
          path: Beliy_Mys_VAC/jobs_supabase.csv

      - name: Загрузка данных в Supabase
        run: python upload_to_supabase.py
        working-directory: Beliy_Mys_VAC

      - name: Upload vacancies_rows.csv
        uses: actions/upload-artifact@v4
        with:
          name: friendwork-vacancies-rows
          path: Beliy_Mys_VAC/vacancies_rows.csv
